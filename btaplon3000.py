#`: trục Ox >3000 và OY<4000
import random
listpoint=[]
for index in range(10000):
    x=random.randint(3000,10000)
    y=random.randint(1,10000)
    point=(x,y)
    print(point)
    listpoint.append(point)
print(listpoint)
#liet ke cac giatri khi chieu tren truc Ox và Oy đều là số nguyên tố:
import random
listpoint=[]
for index in range(1000):
    x=random.randint(1,1000)
    y=random.randint(1,1000)
    point=(x,y)
    if 
    listpoint.append(point)
print(listpoint)